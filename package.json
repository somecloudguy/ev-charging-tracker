{
  "name": "ev-charging-app",
  "version": "1.0.0",
  "description": "Electric vehicle charging data app",
  "scripts": {
    "start": "streamlit run app.py",
    "build": "echo 'No build step required'",
    "test": "echo 'No tests'"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/identity": "^4.0.0"
  },
  "devDependencies": {}
}